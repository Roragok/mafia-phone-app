<!--
  ~ Copyright 2019 Roragok. All Rights Reserved.
  -->

<resources>
    
    <!--        -->
    <!-- themes -->
    <!--        -->
    
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="searchViewStyle">@style/SearchView</item>
    </style>
    
    <!-- toolbar themes -->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="searchViewStyle">@style/SearchView</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:tint">@android:color/white</item>
        <item name="colorControlHighlight">@color/controlPressed</item>
    </style>
    
    <style name="ToolbarPopupTheme" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="android:background">@android:color/white</item>
        <item name="colorControlHighlight">@color/controlPressed</item>
    </style>
    
    <!--                -->
    <!-- control styles -->
    <!--                -->
    
    <style name="SearchView" parent="Widget.AppCompat.Light.SearchView">
        <item name="closeIcon">@drawable/ic_close</item>
        <item name="searchIcon">@drawable/ic_search_white</item>
        <item name="searchHintIcon">@null</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorHighlight">@color/textColorHighlight</item> <!-- text selection color -->
        <item name="android:colorControlActivated">@android:color/white</item> <!-- cursor and selection handle -->
    </style>

</resources>
